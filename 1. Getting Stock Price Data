#The bellw code will help us get a specfic stock data such as High,Low,Open,Close,Volume 

import datetime as dt
import matplotlib.pyplot as plt
from matplotlib import style
import pandas as pd
import pandas_datareader.data as web


style.use('ggplot')
start = dt.datetime(2021,1,1)
end = dt.datetime(2021,1,13)

df = web.DataReader('TSLA','yahoo',start,end)
print(df.head(10))
#You can change how many head you want to print dates

#Then we run the code can get the whole stock data of Tesla in a specfic dates. 


#You can also export a csv file for you data
df.to_csv('tsla.csv')
