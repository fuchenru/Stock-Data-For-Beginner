# Last, we get the data and export to csv
# Now, we are going to import the csv back and see how to do that

import datetime as dt
import matplotlib.pyplot as plt
from matplotlib import style
import pandas as pd
import pandas_datareader.data as web


style.use('ggplot')
# start = dt.datetime(2021,1,1)
# end = dt.datetime(2021,1,13)

# df = web.DataReader('TSLA','yahoo',start,end)
# df.to_csv('tsla.csv')


df = pd.read_csv('tsla.csv', parse_dates=True, index_col=0)
print(df.head())
#This will allow us to get the data back from csv to Python
----------------------------------------------------------------------------------------

#We can also graphing our data!
#This will let us graph the adj close price of the csv
#You can graph whatever you want of the head of the data

df['Adj Close'].plot()
plt.show()
